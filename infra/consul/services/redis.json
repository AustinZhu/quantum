{
  "service": {
    "id": "redis",
    "name": "redis",
    "address": "redis",
    "port": 6379,
    "tags": ["infra", "cache", "queue"],
    "checks": [
      {
        "id": "redis-tcp",
        "name": "redis tcp",
        "tcp": "redis:6379",
        "interval": "10s",
        "timeout": "3s"
      }
    ]
  }
}
